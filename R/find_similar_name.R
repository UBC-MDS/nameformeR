#' Generate the a random set of 10 (by default) suggested similar baby names based on the syllable of the input name.
#'
#' @param name A character vector that is the given name used to find similar names based on syllable
#' @param limit A double vector that is the number of name in the output list. Default 10
#'
#' @return A list contains random suggested similar names based on the given name.
#' @export
#'
#' @examples
#' find_similar_name('Rowan')
find_similar_name <- function(name, limit=10) {
  TRUE
}
